@using AgroPlan.Web.Models.Season
@model String

@{
    ViewData["Title"] = "Sezon ";
}

<!--- Content -->
<div class="row">
    <partial name="_MenuPartial" />
    <div class="col-md-10">
        <div class="container-md mx-4">
            <h2 class="text-center">@ViewData["Title"]</h2>
            <hr />

            <!-- Status message -->
            @if (TempData["Message"] != null)
            {
                <partial name="_StatusMessage" model="@TempData["Message"].ToString()" />
            }
        <div class="text-center">
            @Html.ActionLink("Wnioski", "Index","Application", new { seasonId = Model }, new { @class = "btn btn-primary" })
            @Html.ActionLink("Uprawy", "Index","Plan", new { seasonId = Model }, new { @class = "btn btn-default" })
            @Html.ActionLink("Podsumowanie", "Summary", new { seasonId = Model }, new { @class = "btn btn-success" })
            @Html.ActionLink("Raport", "Raport", new { seasonId = Model }, new { @class = "btn btn-info" })
        </div>
        </div>
    </div>
</div>
<!--- End content -->


